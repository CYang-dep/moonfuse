name = "cyang/moonfuse"

version = "0.1.0"

readme = "README.md"

license = "Apache-2.0"

keywords = [ "fuse", "filesystem", "linux", "kernel", "native" ]

description = "Linux FUSE userspace filesystem protocol library for MoonBit"

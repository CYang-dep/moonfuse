# 示例规划

## 1. 只读内存文件系统
用固定的 inode 树实现 `lookup`、`getattr`、`readdir`，用于教学和协议回归测试。

## 2. 归档视图
将 ZIP/TAR 元数据映射为只读目录树，展示 MoonFuse 如何作为通用用户态文件系统接口。

## 3. 动态配置文件系统
把运行时配置导出为文件和目录，验证句柄、错误映射与并发会话边界。

示例均依赖 MoonFuse 的协议层，不把业务文件系统逻辑塞回核心库。
